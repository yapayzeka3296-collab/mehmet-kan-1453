# Build diagnostic

The cPanel deployment runs `npm run build` and currently fails with Node/V8 exit code -6 (JavaScript heap out of memory). This file is intentionally informational only and does not alter the build configuration.
