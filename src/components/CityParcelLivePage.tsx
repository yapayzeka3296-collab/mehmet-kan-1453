import { useEffect, useState } from "react";
import { ArrowLeft, ShoppingCart } from "lucide-react";
import { supabaseBrowser } from "@/lib/supabaseBrowser";
import { useAuth } from "@/hooks/useAuth";
import { readParcelCart, writeParcelCart, removeParcelFromCart, type ParcelCartItem } from "@/lib/parcelCart";
import type { ParcelTier } from "@/types/parcel";

type City = { id: string; name: string; slug: string };
type MapParcel = { id: string; parcel_number: string; status: "available" | "reserved" | "sold"; tier: ParcelTier; tier_price: number; city_name: string; city_slug: string; grid_x: number | null; grid_y: number | null };
type Slot = { parcel: MapParcel; tier: ParcelTier };
const TIERS: ParcelTier[] = ["digital", "elite", "premium"];
const PER_TIER = 20;
const VISIBLE_COUNT = 60;
const COLS = 12;
const ROWS = 5;
const MAP_IMAGE = "/images/cities/turkey-3d-map.png";
const TIER_COLOR: Record<ParcelTier, string> = { digital: "85,201,255", elite: "183,124,255", premium: "246,196,83" };
const PRICES: Record<ParcelTier, number> = { digital: 199, elite: 499, premium: 999 };
function toCartItem(p: MapParcel): ParcelCartItem { return { id: p.id, parcel_number: p.parcel_number, city_name: p.city_name, tier: p.tier, tier_price: Number(p.tier_price ?? PRICES[p.tier]) }; }
async function loadPublicParcels(citySlug: string): Promise<MapParcel[]> { if (!supabaseBrowser) throw new Error("Supabase bağlantısı bulunamadı."); const { data, error } = await supabaseBrowser.rpc("parcels_in_view", { p_city_slug: citySlug, p_min_lat: -90, p_min_lng: -180, p_max_lat: 90, p_max_lng: 180 }); if (error) throw error; return ((data ?? []) as MapParcel[]).filter(p => p.status === "available"); }
export function CityParcelLivePage({ slug }: { slug: string }) {
  const { user, loading: authLoading } = useAuth();
  const [city, setCity] = useState<City | null>(null);
  const [slots, setSlots] = useState<Array<Slot | null>>([]);
  const [selected, setSelected] = useState<ParcelCartItem[]>(() => readParcelCart());
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);
  useEffect(() => { let alive = true; async function load() { setLoading(true); setError(null); setSlots([]); setCity(null); try { if (!supabaseBrowser) throw new Error("Supabase bağlantısı bulunamadı."); const { data: cityData, error: cityError } = await supabaseBrowser.from("cities").select("id,name,slug").eq("slug", slug).eq("is_active", true).maybeSingle(); if (cityError) throw cityError; if (!cityData) throw new Error("İl bulunamadı."); if (!alive) return; setCity(cityData as City); const existing = readParcelCart(); setSelected(existing); const selectedIds = new Set(existing.map(p => p.id)); const rows = (await loadPublicParcels(slug)).filter(p => !selectedIds.has(p.id)); const next: Array<Slot | null> = []; for (const tier of TIERS) { const tierRows = rows.filter(p => p.tier === tier).slice(0, PER_TIER); for (const parcel of tierRows) next.push({ parcel, tier }); while (next.length < (TIERS.indexOf(tier) + 1) * PER_TIER) next.push(null); } if (alive) setSlots(next.slice(0, VISIBLE_COUNT)); } catch (e) { if (alive) setError(e instanceof Error ? e.message : "Parseller yüklenemedi."); } finally { if (alive) setLoading(false); } } void load(); return () => { alive = false; }; }, [slug]);
  const handleSelect = (slotIndex: number) => { const slot = slots[slotIndex]; if (!slot || slot.parcel.status !== "available" || busy) return; const item = toCartItem(slot.parcel); if (selected.some(x => x.id === item.id)) return; const nextSelected = [...selected, item]; setSelected(nextSelected); writeParcelCart(nextSelected); };
  const handleDeselect = (item: ParcelCartItem) => { const next = selected.filter(x => x.id !== item.id); setSelected(next); removeParcelFromCart(item.id); };
  const activateSlot = (slotIndex: number) => { const slot = slots[slotIndex]; if (!slot || slot.parcel.status !== "available" || busy) return; const isSelected = selected.some(x => x.id === slot.parcel.id); if (isSelected) handleDeselect(toCartItem(slot.parcel)); else handleSelect(slotIndex); };
  const selectedIds = new Set(selected.map(p => p.id));
  const total = selected.reduce((sum, p) => sum + Number(p.tier_price ?? 0), 0);
  const buy = () => { if (authLoading || selected.length === 0) return; const ids = selected.map(p => p.id).join(","); window.location.href = user ? `/parsel-satin-al?parcels=${ids}` : `/giris?redirect=${encodeURIComponent(`/parsel-satin-al?parcels=${ids}`)}`; };
  if (!city) return <main className="mx-auto max-w-4xl p-8 text-center text-white"><h1 className="text-2xl font-bold">{loading ? "Harita hazırlanıyor…" : "İl bulunamadı"}</h1>{error && <p className="mt-3 text-red-300">{error}</p>}<a href="/turkiye-haritasi" className="mt-4 inline-flex items-center gap-2 text-cyan-300"><ArrowLeft className="h-4 w-4" /> Türkiye haritasına dön</a></main>;
  return <main className="mx-auto max-w-[1800px] px-3 py-4 text-white sm:px-5 lg:px-8"><a href="/turkiye-haritasi" className="mb-4 inline-flex items-center gap-2 text-sm text-cyan-200/80"><ArrowLeft className="h-4 w-4" /> Türkiye haritası</a><section className="overflow-hidden rounded-3xl border border-cyan-200/20 bg-slate-900/90 shadow-2xl"><div className="relative min-h-[380px] overflow-hidden bg-[#020914] sm:min-h-[650px]"><img src={MAP_IMAGE} alt="Türkiye gökyüzü parsel haritası" className="pointer-events-none absolute inset-0 z-0 h-full w-full object-contain opacity-90" /><div className="pointer-events-none absolute inset-0 z-10 bg-[radial-gradient(circle_at_50%_45%,rgba(30,150,220,.22),transparent_42%),linear-gradient(145deg,rgba(2,7,17,.55),rgba(7,26,45,.18),rgba(1,4,11,.55)]" /><div className="pointer-events-auto absolute inset-4 z-[100] grid gap-1.5 sm:inset-8" style={{ gridTemplateColumns: `repeat(${COLS},minmax(0,1fr))`, gridTemplateRows: `repeat(${ROWS},minmax(0,1fr))`, touchAction: "manipulation" }}>{Array.from({ length: VISIBLE_COUNT }, (_, i) => { const slot = slots[i]; if (!slot) return <div key={`empty-${i}`} className="pointer-events-none rounded-sm border-[3px] border-cyan-100/45 bg-cyan-100/[0.04]" />; const p = slot.parcel; const isSelected = selectedIds.has(p.id); const rgb = TIER_COLOR[p.tier]; return <button key={p.id} type="button" onClick={(event) => { event.preventDefault(); event.stopPropagation(); activateSlot(i); }} className="relative z-[110] block h-full w-full min-h-0 min-w-0 cursor-pointer select-none rounded-sm border-[3px] p-0 transition-all duration-150 hover:brightness-150 active:scale-95 focus:outline-none focus:ring-2 focus:ring-white" aria-pressed={isSelected} aria-label={`${p.parcel_number} ${p.tier} parseli ${isSelected ? "seçildi, kaldır" : "seç"}`} style={{ WebkitTapHighlightColor: "transparent", WebkitAppearance: "none", borderColor: `rgba(${rgb},1)`, backgroundColor: isSelected ? `rgba(${rgb},.72)` : `rgba(${rgb},.20)`, boxShadow: isSelected ? `0 0 18px rgba(${rgb},1),0 0 40px rgba(${rgb},.95),inset 0 0 20px rgba(${rgb},.9)` : `0 0 7px rgba(${rgb},.7),inset 0 0 0 1px rgba(${rgb},.5)`, touchAction: "manipulation", pointerEvents: "auto" }}><span className="pointer-events-none absolute inset-[8%] rounded-sm" style={{ background: `rgba(${rgb},${isSelected ? ".55" : ".18"})` }} /><span className="pointer-events-none absolute inset-0 flex items-center justify-center text-[6px] font-semibold text-white/80 sm:text-[9px]">{p.parcel_number}</span>{isSelected && <span className="pointer-events-none absolute inset-0 animate-pulse rounded-sm" style={{ boxShadow: `inset 0 0 20px rgba(${rgb},1)` }} />}</button>; })}</div><div className="pointer-events-none absolute bottom-5 left-5 z-20"><p className="text-xs uppercase tracking-[.2em] text-cyan-200/70">MySkyParcel · Türkiye Gökyüzü Parsel Haritası</p><h1 className="mt-1 text-3xl font-bold">{city.name}</h1><p className="mt-1 text-sm text-white/70">Mavi: Dijital · Mor: Elit · Altın: Premium · Kareye dokunarak parseli seçin.</p></div></div><div className="grid gap-3 p-4 sm:grid-cols-4"><div className="rounded-xl border border-cyan-300/15 bg-slate-950/60 p-3"><span className="text-xs text-white/45">Ekrandaki parsel</span><div className="text-xl font-bold text-cyan-200">{slots.filter(Boolean).length}</div></div><div className="rounded-xl border border-cyan-300/15 bg-slate-950/60 p-3"><span className="text-xs text-white/45">Seçilen</span><div className="text-xl font-bold text-amber-200">{selected.length}</div></div><div className="rounded-xl border border-cyan-300/15 bg-slate-950/60 p-3"><span className="text-xs text-white/45">Veri durumu</span><div className="text-sm font-semibold text-emerald-300">{loading ? "Yükleniyor…" : busy ? "Yeni parsel getiriliyor…" : "Canlı"}</div></div><button type="button" onClick={buy} disabled={!selected.length || authLoading} className="inline-flex items-center justify-center gap-2 rounded-xl bg-cyan-400 px-4 py-3 font-bold text-slate-950 disabled:opacity-40"><ShoppingCart className="h-4 w-4" /> Satın almaya devam et · {total.toLocaleString("tr-TR")} ₺</button></div>{error && <div className="mx-4 mb-4 rounded-xl border border-red-400/30 bg-red-950/30 p-3 text-sm text-red-200">Parsel verisi: {error}</div>}</section></main>;
}
