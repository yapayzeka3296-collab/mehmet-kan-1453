import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { ArrowRight, Check, FileBadge2, Loader2, Sparkles } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";
import { TrustBar } from "@/components/TrustBar";
import { supabaseBrowser } from "@/lib/supabaseBrowser";
import { readParcelCart } from "@/lib/parcelCart";
import type { ParcelTier } from "@/types/parcel";

type Tier = ParcelTier;
type PurchaseItem = { id: string; tier: Tier; price: number };
type PurchaseSearch = { parcels?: string; certificateParcel?: string };
export const Route = createFileRoute("/parsel-satin-al")({ validateSearch: (search: Record<string, unknown>): PurchaseSearch => ({ parcels: typeof search.parcels === "string" ? search.parcels : undefined, certificateParcel: typeof search.certificateParcel === "string" ? search.certificateParcel : undefined }), head: () => ({ meta: [{ title: "Parsel Satın Al — MySkyParcel" }, { name: "description", content: "İstediğiniz sayıda sembolik parsel seçin ve yalnızca bir parsel için sertifika talep edin." }] }), component: SatinAl });
const PRICES: Record<Tier, number> = { digital: 149, elite: 349, premium: 699 };
const NAMES: Record<Tier, string> = { digital: "Dijital", elite: "Özel", premium: "Premium" };
const PHYSICAL: Record<Tier, boolean> = { digital: false, elite: true, premium: true };
function SatinAl() {
  const navigate = useNavigate({ from: "/parsel-satin-al" });
  const { parcels, certificateParcel: requestedCertificate } = Route.useSearch();
  const [cartIds, setCartIds] = useState<string[]>([]);
  useEffect(() => {
    if (parcels) return;
    setCartIds(readParcelCart().map((item) => item.id));
  }, [parcels]);
  const ids = useMemo(() => Array.from(new Set(((parcels ?? cartIds.join(","))).split(",").map((v) => v.trim()).filter(Boolean))), [parcels, cartIds]);
  const [items, setItems] = useState<PurchaseItem[]>([]); const [loading, setLoading] = useState(true); const [validationError, setValidationError] = useState<string | null>(null); const [certificateParcel, setCertificateParcel] = useState(""); const [accepted, setAccepted] = useState(false); const [name, setName] = useState(""); const [phone, setPhone] = useState(""); const [address, setAddress] = useState(""); const [city, setCity] = useState(""); const [district, setDistrict] = useState("");
  useEffect(() => { let active = true; async function validateParcels() { if (!ids.length) { if (active) { setItems([]); setLoading(false); } return; } if (!supabaseBrowser) { if (active) { setValidationError("Parsel doğrulama altyapısı kullanılamıyor."); setLoading(false); } return; } setLoading(true); setValidationError(null); const { data, error } = await supabaseBrowser.from("parcel_map_public").select("id,tier,tier_price,status").in("id", ids); if (!active) return; if (error) { setValidationError("Parseller doğrulanamadı. Lütfen tekrar deneyin."); setItems([]); setLoading(false); return; } const rows = (data ?? []) as Array<{ id: string; tier: Tier | null; tier_price: number | null; status: string }>; const byId = new Map(rows.map((row) => [row.id, row])); const missing = ids.filter((id) => !byId.has(id)); const unavailable = rows.filter((row) => row.status !== "available"); const invalid = rows.filter((row) => !row.tier || !["digital", "elite", "premium"].includes(row.tier) || Number(row.tier_price) !== PRICES[row.tier!]); if (missing.length || unavailable.length || invalid.length) { setValidationError(unavailable.length ? `Şu parseller artık satışa uygun değil: ${unavailable.map((row) => row.id).join(", ")}` : invalid.length ? "Parsel fiyatı paket fiyatıyla eşleşmiyor; ödeme durduruldu." : "Seçilen parseller doğrulanamadı."); setItems([]); setLoading(false); return; } const next = ids.map((id) => { const row = byId.get(id)!; return { id, tier: row.tier!, price: PRICES[row.tier!] }; }); setItems(next); setCertificateParcel((current) => current && ids.includes(current) ? current : (requestedCertificate && ids.includes(requestedCertificate) ? requestedCertificate : (ids[0] ?? ""))); setLoading(false); } void validateParcels(); return () => { active = false; }; }, [ids, requestedCertificate]);
  const certificateTier = certificateParcel ? items.find((item) => item.id === certificateParcel)?.tier ?? "digital" : "digital"; const physicalReady = !PHYSICAL[certificateTier] || Boolean(name.trim() && phone.trim() && address.trim() && city.trim() && district.trim()); const total = items.reduce((sum, item) => sum + item.price, 0); const grouped = items.reduce<Record<Tier, number>>((acc, item) => { acc[item.tier] += 1; return acc; }, { digital: 0, elite: 0, premium: 0 });
  function handleContinue() { if (!items.length || !certificateParcel || !accepted || !physicalReady) return; localStorage.setItem("myskyparcel_purchase_plan", JSON.stringify({ items, certificateParcel })); if (PHYSICAL[certificateTier]) localStorage.setItem("myskyparcel_delivery", JSON.stringify({ name: name.trim(), phone: phone.trim(), address: address.trim(), city: city.trim(), district: district.trim() })); void navigate({ to: "/odeme", search: { parcels: items.map((item) => item.id).join(","), certificateParcel } }); }
  if (loading) return <div className="starfield min-h-screen"><SiteHeader /><main className="mx-auto max-w-3xl px-4 py-16 lg:px-8"><div className="panel p-8 text-center"><Loader2 className="mx-auto h-7 w-7 animate-spin text-gold" /><p className="mt-4 text-sm text-muted-foreground">Parseller ve paket türleri doğrulanıyor...</p></div></main><SiteFooter /></div>;
  if (!items.length) return <div className="starfield min-h-screen"><SiteHeader /><main className="mx-auto max-w-3xl px-4 py-16 lg:px-8"><div className="panel p-8 text-center"><h1 className="font-display text-3xl font-bold">SATIN ALMA HAZIRLANAMADI</h1><p className="mt-3 text-sm text-muted-foreground">{validationError ?? "Sepetinizde ödeme için uygun parsel bulunamadı."}</p><button type="button" onClick={() => void navigate({ to: "/gokyuzu-haritasi" })} className="btn-gold mt-6 rounded-md px-6 py-3 text-xs">HARİTAYA DÖN</button></div></main><SiteFooter /></div>;
  return <div className="starfield min-h-screen"><SiteHeader /><main className="mx-auto max-w-[1200px] px-4 py-12 lg:px-8"><h1 className="text-center font-display text-4xl font-bold sm:text-5xl">SATIN ALMA</h1><p className="mx-auto mt-3 max-w-2xl text-center text-sm text-muted-foreground">İstediğiniz sayıda ve farklı türlerde parsel seçebilirsiniz. Bu siparişte yalnızca <strong className="text-foreground">bir parsel için sertifika</strong> talep edilebilir.</p><div className="mt-10 grid gap-6 lg:grid-cols-[1fr_380px]"><section className="panel p-6 sm:p-8"><h2 className="font-display text-lg">SEÇİLEN PARSELLER ({items.length})</h2><div className="mt-5 space-y-3">{items.map((item) => <div key={item.id} className={`rounded-xl border p-4 ${certificateParcel === item.id ? "border-gold/50 bg-gold/[0.05]" : "border-border"}`}><div className="flex items-center justify-between gap-4"><div><p className="font-medium">{item.id}</p><p className="mt-1 text-xs text-muted-foreground">{NAMES[item.tier]} · {item.price.toLocaleString("tr-TR")} TL</p></div><label className="flex shrink-0 cursor-pointer items-center gap-2 text-xs text-gold"><input type="radio" name="certificateParcel" checked={certificateParcel === item.id} onChange={() => setCertificateParcel(item.id)} /><FileBadge2 className="h-4 w-4" /> Sertifika bu parsele</label></div></div>)}</div><div className="mt-6 rounded-xl border border-gold/20 bg-gold/[0.04] p-5"><p className="text-sm font-semibold text-gold">Tek sertifika kuralı</p><p className="mt-2 text-xs leading-5 text-muted-foreground">Siparişte kaç parsel olursa olsun yalnızca seçtiğiniz <strong className="text-foreground">{certificateParcel}</strong> için sertifika talebi oluşturulur.</p></div>{PHYSICAL[certificateTier] && <div className="mt-6 rounded-xl border border-gold/20 bg-gold/[0.04] p-5"><p className="text-sm font-semibold text-gold">FİZİKSEL SERTİFİKA TESLİMATI</p><p className="mt-2 text-xs leading-5 text-muted-foreground">Seçtiğiniz sertifika {NAMES[certificateTier]} paketine ait olduğu için fiziksel sertifika hazırlanır ve verdiğiniz adrese gönderilir.</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><input value={name} onChange={(e) => setName(e.target.value)} placeholder="Ad Soyad" autoComplete="name" className="rounded-md border border-border bg-background/40 px-3 py-3 text-sm" /><input value={phone} onChange={(e) => setPhone(e.target.value)} placeholder="Telefon" autoComplete="tel" className="rounded-md border border-border bg-background/40 px-3 py-3 text-sm" /><input value={city} onChange={(e) => setCity(e.target.value)} placeholder="İl" autoComplete="address-level1" className="rounded-md border border-border bg-background/40 px-3 py-3 text-sm" /><input value={district} onChange={(e) => setDistrict(e.target.value)} placeholder="İlçe" autoComplete="address-level2" className="rounded-md border border-border bg-background/40 px-3 py-3 text-sm" /><textarea value={address} onChange={(e) => setAddress(e.target.value)} placeholder="Açık teslimat adresi" autoComplete="street-address" className="min-h-24 rounded-md border border-border bg-background/40 px-3 py-3 text-sm sm:col-span-2" /></div></div>}<label className="mt-6 flex cursor-pointer items-start gap-3 text-xs text-muted-foreground"><input type="checkbox" checked={accepted} onChange={(e) => setAccepted(e.target.checked)} className="mt-0.5 accent-current" /><span>Seçtiğim parselleri, sertifika talep ettiğim tek parseli ve satış sözleşmelerine eriştiğimi onaylıyorum.</span></label><button type="button" disabled={!accepted || !physicalReady} onClick={handleContinue} className="btn-gold mt-5 inline-flex w-full items-center justify-center gap-2 rounded-md px-6 py-3 text-[11px] disabled:cursor-not-allowed disabled:opacity-50"><Sparkles className="h-4 w-4" /> ÖDEMEYE GEÇ <ArrowRight className="h-4 w-4" /></button></section><aside className="panel h-fit p-6 lg:sticky lg:top-6"><h2 className="font-display text-base">SİPARİŞ ÖZETİ</h2><div className="mt-5 space-y-3 text-sm">{(["digital", "elite", "premium"] as Tier[]).filter((tier) => grouped[tier] > 0).map((tier) => <div key={tier} className="flex justify-between"><span className="text-muted-foreground">{NAMES[tier]} × {grouped[tier]}</span><span>{(PRICES[tier] * grouped[tier]).toLocaleString("tr-TR")} TL</span></div>)}<div className="border-t border-border pt-4 flex justify-between"><span className="font-semibold">Toplam</span><span className="font-display text-2xl text-gold">{total.toLocaleString("tr-TR")} TL</span></div><div className="border-t border-border pt-4 text-xs text-muted-foreground"><p><strong className="text-foreground">Sertifika:</strong> {certificateParcel}</p><p className="mt-1">Toplam parsel: {items.length}</p></div></div><ul className="mt-5 space-y-3 text-sm">{["İstediğiniz sayıda sembolik parsel", "Farklı paket türlerini aynı siparişte seçebilme", "Yalnızca bir parsel için sertifika talebi", "Her parsel için benzersiz kayıt"].map((item) => <li key={item} className="flex items-start gap-2 text-muted-foreground"><Check className="mt-0.5 h-4 w-4 shrink-0 text-gold" />{item}</li>)}</ul></aside></div></main><TrustBar /><SiteFooter /></div>;
}
