import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { ArrowLeft, ExternalLink, Loader2, Lock, ShieldCheck } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";
import { supabaseBrowser } from "@/lib/supabaseBrowser";
import type { ParcelTier } from "@/types/parcel";

type Tier = ParcelTier;
type PaymentGroup = { tier: Tier; name: string; price: number; link: string; count: number; parcels: string[] };
export const Route = createFileRoute("/odeme")({ validateSearch: (search: Record<string, unknown>) => ({ parcels: typeof search.parcels === "string" ? search.parcels : "", certificateParcel: typeof search.certificateParcel === "string" ? search.certificateParcel : "" }), head: () => ({ meta: [{ title: "Ödeme — MySkyParcel" }, { name: "description", content: "MySkyParcel çoklu parsel ödeme planı." }] }), component: Odeme });
const PACKAGES: Record<Tier, { name: string; price: number; link: string }> = { digital: { name: "Dijital", price: 199, link: import.meta.env.VITE_IYZICO_DIGITAL_LINK?.trim() ?? "" }, elite: { name: "Özel", price: 499, link: import.meta.env.VITE_IYZICO_ELITE_LINK?.trim() ?? "" }, premium: { name: "Premium", price: 999, link: import.meta.env.VITE_IYZICO_PREMIUM_LINK?.trim() ?? "" } };
function Odeme() {
  const navigate = useNavigate({ from: "/odeme" }); const { parcels, certificateParcel } = Route.useSearch(); const selectedParcels = useMemo(() => Array.from(new Set(parcels.split(",").map((value) => value.trim()).filter(Boolean))), [parcels]);
  const [verified, setVerified] = useState<Array<{ id: string; tier: Tier; price: number }>>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null);
  useEffect(() => { let active = true; async function verify() { if (!selectedParcels.length) { setVerified([]); setLoading(false); return; } if (!supabaseBrowser) { setError("Parsel doğrulama altyapısı kullanılamıyor."); setLoading(false); return; } setLoading(true); setError(null); const { data, error: queryError } = await supabaseBrowser.from("parcel_map_public").select("id,tier,tier_price,status").in("id", selectedParcels); if (!active) return; if (queryError) { setError("Parseller ödeme öncesinde doğrulanamadı."); setVerified([]); setLoading(false); return; } const rows = (data ?? []) as Array<{ id: string; tier: Tier | null; tier_price: number | null; status: string }>; const byId = new Map(rows.map((row) => [row.id, row])); const missing = selectedParcels.filter((id) => !byId.has(id)); const unavailable = rows.filter((row) => row.status !== "available"); const invalid = rows.filter((row) => !row.tier || !["digital", "elite", "premium"].includes(row.tier) || Number(row.tier_price) !== PACKAGES[row.tier!].price); if (missing.length || unavailable.length || invalid.length) { setError(unavailable.length ? `Ödeme durduruldu. Satışa uygun olmayan parsel: ${unavailable.map((row) => row.id).join(", ")}` : invalid.length ? "Parsel paket/fiyat bilgisi doğrulanamadı; ödeme durduruldu." : "Seçilen parseller doğrulanamadı."); setVerified([]); setLoading(false); return; } setVerified(selectedParcels.map((id) => { const row = byId.get(id)!; const tier = row.tier!; return { id, tier, price: PACKAGES[tier].price }; })); setLoading(false); } void verify(); return () => { active = false; }; }, [selectedParcels]);
  const groups = useMemo(() => (["digital", "elite", "premium"] as Tier[]).map((tier): PaymentGroup | null => { const tierItems = verified.filter((item) => item.tier === tier); if (!tierItems.length) return null; return { ...PACKAGES[tier], tier, count: tierItems.length, parcels: tierItems.map((item) => item.id) }; }).filter((group): group is PaymentGroup => Boolean(group)), [verified]);
  const total = verified.reduce((sum, item) => sum + item.price, 0); const missingLink = groups.some((group) => !group.link); const certificateIsSelected = Boolean(certificateParcel && selectedParcels.includes(certificateParcel));
  useEffect(() => { if (!verified.length || !certificateIsSelected || typeof window === "undefined") return; try { window.localStorage.setItem("myskyparcel_checkout_plan", JSON.stringify({ items: verified, certificateParcel, groups, total })); } catch { /* storage unavailable */ } }, [verified, certificateParcel, groups, total, certificateIsSelected]);
  if (loading) return <div className="starfield min-h-screen"><SiteHeader /><main className="mx-auto max-w-3xl px-4 py-16 lg:px-8"><div className="panel p-8 text-center"><Loader2 className="mx-auto h-7 w-7 animate-spin text-gold" /><p className="mt-4 text-sm text-muted-foreground">Ödeme öncesi parseller doğrulanıyor...</p></div></main><SiteFooter /></div>;
  if (!verified.length || !certificateIsSelected) return <div className="starfield min-h-screen"><SiteHeader /><main className="mx-auto max-w-3xl px-4 py-16 lg:px-8"><div className="panel p-8 text-center"><h1 className="font-display text-3xl font-bold">ÖDEME HAZIRLANAMADI</h1><p className="mt-3 text-sm text-muted-foreground">{error ?? (!certificateIsSelected ? "Sertifika için bir parsel seçimi doğrulanamadı." : "Seçilen parseller doğrulanamadı.")}</p><button type="button" onClick={() => void navigate({ to: "/parsel-satin-al", search: { parcels: selectedParcels.join(","), certificateParcel: certificateIsSelected ? certificateParcel : selectedParcels[0] } })} className="mt-6 inline-flex items-center gap-2 rounded-md border border-border px-6 py-3 text-xs"><ArrowLeft className="h-4 w-4" /> BİLGİLERE DÖN</button></div></main><SiteFooter /></div>;
  return <div className="starfield min-h-screen"><SiteHeader /><main className="mx-auto max-w-[1200px] px-4 py-12 lg:px-8"><h1 className="text-center font-display text-4xl font-bold sm:text-5xl">ÖDEME</h1><p className="mx-auto mt-3 max-w-3xl text-center text-sm text-muted-foreground">Seçtiğiniz {verified.length} parsel doğrulandı. Sertifika yalnızca <strong className="text-foreground">{certificateParcel}</strong> için talep edilir.</p><div className="mt-10 grid gap-6 lg:grid-cols-[1fr_360px]"><section className="panel p-6 sm:p-8"><div className="mb-6 flex items-center gap-3"><Lock className="h-5 w-5 text-gold" /><div><h2 className="font-display text-lg">İYZİCO LİNK İLE ÖDEME</h2><p className="text-xs text-muted-foreground">Kart bilgileri MySkyParcel üzerinde alınmaz. Ödeme, seçtiğiniz paket için İyzico'nun güvenli ödeme sayfasında tamamlanır.</p></div></div><div className="rounded-xl border border-gold/20 bg-gold/[0.04] p-5"><p className="text-sm font-semibold text-gold">Doğrulanmış parsel siparişi</p><p className="mt-2 text-xs leading-5 text-muted-foreground">Paket türü, satış durumu ve fiyat MySkyParcel kayıtlarından ödeme öncesinde yeniden doğrulandı.</p></div><div className="mt-5 space-y-4">{groups.map((group) => <div key={group.tier} className="rounded-xl border border-border p-5"><div className="flex items-center justify-between gap-4"><div><p className="font-semibold">{group.name}</p><p className="mt-1 text-xs text-muted-foreground">{group.count} parsel × {group.price.toLocaleString("tr-TR")} TL</p></div><p className="font-display text-xl text-gold">{(group.price * group.count).toLocaleString("tr-TR")} TL</p></div><div className="mt-3 text-xs text-muted-foreground">{group.parcels.join(", ")}</div><div className="mt-4 flex items-center justify-between gap-3"><span className="text-[11px] text-muted-foreground">{group.count > 1 ? `${group.count} Link ödemesi gerekir.` : "1 Link ödemesi gerekir."}</span>{group.link ? <a href={group.link} target="_blank" rel="noopener noreferrer" className="btn-gold inline-flex shrink-0 items-center gap-2 rounded-md px-5 py-3 text-[11px]"><Lock className="h-4 w-4" /> İYZİCO'DA ÖDE <ExternalLink className="h-4 w-4" /></a> : <button disabled className="btn-gold rounded-md px-5 py-3 text-[11px] opacity-50">ÖDEME LİNKİ HAZIR DEĞİL</button>}</div></div>)}</div>{missingLink && <div className="mt-4 rounded-xl border border-destructive/40 bg-destructive/5 p-4 text-xs">Seçilen paketlerden en az birinin canlı İyzico Link'i Vercel ortam değişkenine eklenmemiş.</div>}<div className="mt-5 rounded-xl border border-border p-5 text-xs text-muted-foreground"><p className="font-semibold text-foreground">Ödeme sonrası</p><p className="mt-2">İyzico Link ile tamamlanan ödeme, İyzico Kontrol Paneli üzerinden takip edilir. Parseller ödeme doğrulanana kadar kesin olarak satılmış kabul edilmez.</p></div></section><aside className="panel h-fit p-6 lg:sticky lg:top-6"><h2 className="font-display text-base">SİPARİŞ ÖZETİ</h2><div className="mt-5 space-y-3 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Parsel sayısı</span><span>{verified.length}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Sertifika</span><span>{certificateParcel}</span></div>{groups.map((group) => <div key={group.tier} className="flex justify-between"><span className="text-muted-foreground">{group.name} × {group.count}</span><span>{(group.price * group.count).toLocaleString("tr-TR")} TL</span></div>)}<div className="border-t border-border pt-4 flex justify-between"><span className="font-semibold">Toplam</span><span className="font-display text-2xl text-gold">{total.toLocaleString("tr-TR")} TL</span></div></div><div className="mt-5 rounded-lg border border-gold/20 bg-gold/[0.04] p-4 text-xs text-muted-foreground"><ShieldCheck className="mb-2 h-4 w-4 text-gold" /><p>Parseller ödeme doğrulanana kadar kesin olarak satılmış kabul edilmez.</p></div><button type="button" onClick={() => void navigate({ to: "/parsel-satin-al", search: { parcels: selectedParcels.join(","), certificateParcel } })} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-md border border-border px-5 py-3 text-xs"><ArrowLeft className="h-4 w-4" /> BİLGİLERE DÖN</button></aside></div></main><SiteFooter /></div>;
}
