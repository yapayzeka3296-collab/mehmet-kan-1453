name: Netlen Node.js Production Deploy

# Temporarily disabled while the Netlen Node/Passenger deployment model is being corrected.
# CI remains untouched.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Deployment paused
    runs-on: ubuntu-latest
    steps:
      - run: echo "Netlen automatic deployment is temporarily paused."
